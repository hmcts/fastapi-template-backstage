python:
  applicationPort: ${{ values.http_port }}
  image: 'hmctsprod.azurecr.io/${{ values.product }}/${{ values.component }}:latest'
  ingressHost: ${{ values.product }}-${{ values.component }}-{% raw %}{{ .Values.global.environment }}.service.core-compute-{{ .Values.global.environment }}.internal{% endraw %}
  environment:
  # keyVaults:
  #   ${{ values.product }}:
  #     secrets:
  #       - name: AppInsightsConnectionString
  #         alias: APPLICATIONINSIGHTS_CONNECTION_STRING
  # postgresql:
  #   enabled: true
